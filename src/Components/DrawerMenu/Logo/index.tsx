import React from 'react';

import { Container, LogoImage } from './styles';

const Logo: React.FC = () => {
    return (
        <Container>
            <LogoImage />
        </Container>
    );
};

export default Logo;
